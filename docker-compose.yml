services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - VITE_API_URL=${VITE_API_URL:-/api}
    container_name: qr-frontend
    ports:
      - "80:80"
    restart: unless-stopped
